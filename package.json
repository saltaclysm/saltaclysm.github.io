{
  "name": "black-site",
  "version": "1.0.0",
  "description": "Simple two-page black-themed static site",
  "private": true,
  "scripts": {
    "start": "http-server ./ -p 8000 -a 127.0.0.1",
    "dev": "live-server ./ --port=8080 --host=127.0.0.1 --open=./index.html"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "live-server": "^1.2.2"
  }
}
